export const scrollToLoadMoreInSelect = (event) => {
    const target = event.target
    // if (!loading && target.scrollTop + target.offsetHeight === target.scrollHeight) {
    //     setLoading(true)
    //     console.log("Load...")
    //     target.scrollTo(0, target.scrollHeight)
    //     setTimeout(() => {
    //         setChildren((prevState) =>
    //             prevState.concat(
    //                 Array.from({ length: 20 }).map((el, i) => {
    //                     return { label: `label-${i}`, value: `value-${i}` }
    //                 })
    //             )
    //         )
    //         setLoading(false)
    //     }, 1500)
    // }
}
